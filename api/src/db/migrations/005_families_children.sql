-- 005: families and children tables are created via schema.sql (CREATE TABLE IF NOT EXISTS).
-- This migration is intentionally empty — it exists as a marker so the runner records
-- that the schema-level tables were introduced at this point in the sequence.
SELECT 1;
